{/* F6-01 fixture, outside content/es/: the widgets of the T-0.1 spec and a RobotFormula,
written with the names and props a topic uses and rendered with the same MDX map. */}

<RotationWidget mode="disc" inputUnit="rpm" initial={{ omega_radps: 20.94, r_m: 0.032 }} />

<RobotFormula calc="demo/omega-rueda" />

<MyRobotWidget mode="form" />
