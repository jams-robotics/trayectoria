// F7-02b (#444): direct entry (ADR-0009) so pages that only need a toast skip the barrel.
export { TOAST_TIMEOUT_MS, Toast } from '../shared/Toast';
export type { ToastProps, ToastTone } from '../shared/Toast';
